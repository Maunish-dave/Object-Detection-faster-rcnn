# Object-Detection-faster-rcnn
Django Webapp of Object Detection using faster-rcnn.

Model is build using pytorch

copy files of from this repository in your django app.
add a faster-rcnn model in media file. 
(as github does not allow files greater than 25 mb model is not uploaded).

